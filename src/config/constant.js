export const timeout = 20000
export const selfHost = typeof window === 'undefined' ? 'shortink.tech' : window?.location?.host
export const selfOrigin = typeof window === 'undefined' ? 'https://shortink.tech' : window?.location?.origin
export const title = "ShortInk"
export const subtitle = "ShortInk | Short your link, with simplicity and readable URL"
export const description = "A trusted URL shortener with unlimited links created and no payment needed. Try it now, its FREE!"
export const keywords = "shortener, url, link, url shortener, link shortener"
export const image = "/banner.png"